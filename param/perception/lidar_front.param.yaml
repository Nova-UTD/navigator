# Templated from Autoware drivers/velodyne_nodes/param/test.param.yaml
/**:
  ros__parameters:
    ip: "0.0.0.0"
    port: 2369
    cloud_size:  55000
    topic: "points_raw"  # UdpDriverNode strictly expects to get the topic via `declare_parameter`.
    frame_id: "lidar_front"
    timeout_ms: 10
    rpm:        600
